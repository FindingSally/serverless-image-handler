{
  "name": "image-auth",
  "version": "1.0.0",
  "private": true,
  "description": "A Lambda @Edge Function that checks for valid Cognito credentials",
  "license": "Apache-2.0",
  "author": "AWS Solutions",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules/ dist/ coverage/",
    "pretest": "npm run clean && npm ci",
    "test": "jest --coverage --silent"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity": "^3.298.0",
    "@aws-sdk/client-ssm": "^3.299.0",
    "@aws-sdk/credential-provider-cognito-identity": "^3.298.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/jest": "^29.1.1",
    "@types/node": "^18.8.4",
    "jest": "^29.1.2",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
